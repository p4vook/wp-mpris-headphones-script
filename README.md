Wireplumber C script to send a stop signal to all MPRIS players
(using `playerctl -a stop`) upon headphones disconnect.

Currently, only Bluetooth output devices are supported.

Dependencies:
* GLib
* playerctl
* pipewire
* wireplumber
